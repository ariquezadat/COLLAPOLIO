/*
 * Configuración de runtime. Se carga antes que la app, así que cambiar la URL
 * del servidor de partidas es editar este archivo y volver a desplegar el
 * hosting — sin recompilar la web.
 */
window.__COLLAPOLIO_CONFIG__ = {
  serverUrl: 'http://localhost:4000',
};
